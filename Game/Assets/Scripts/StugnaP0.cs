using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StugnaP0 : StugnaP
{
    // Start is called before the first frame update
    void Start()
    {
        curentStugna = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
